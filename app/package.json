{
  "name": "agenda-cloud",
  "version": "0.0.1",
  "description": "A web app that can handle bookings for multiple types of business",
  "scripts": {
    "build": "./build.sh"
  },
  "devDependencies": {
    "gulp-cssnano": "^2.1.3"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "jquery": "^3.6.0"
  }
}
